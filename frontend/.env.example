# API Configuration
VITE_API_URL=https://marketinsight-skgl.onrender.com/api/chat

# API Authentication (Optional)
# Only required if REQUIRE_API_KEY is enabled on the backend
# Leave empty if authentication is disabled (default)
VITE_API_KEY=
