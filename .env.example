# API Security Configuration
# Copy this file to .env and update with your values

# =============================================================================
# API Authentication
# =============================================================================
# API key for protecting endpoints (optional)
# Generate a secure random key for production use
# Example: python -c "import secrets; print(secrets.token_urlsafe(32))"
API_KEY=your-secret-api-key-here

# Require API key for protected endpoints (true/false)
# Set to 'true' to enable authentication on protected endpoints
# Set to 'false' to disable authentication (development mode)
REQUIRE_API_KEY=false

# =============================================================================
# CORS Configuration
# =============================================================================
# Comma-separated list of allowed origins for Cross-Origin Resource Sharing
# In production, restrict to your actual frontend domain(s)
# Format: https://yourdomain.com,https://www.yourdomain.com
CORS_ORIGINS=http://localhost:5173,http://localhost:3000

# =============================================================================
# Rate Limiting
# =============================================================================
# Note: Rate limiting is currently configured in middleware/rate_limiter.py
# Default limit: 100 requests per minute per IP address
# For distributed deployments, modify storage_uri from "memory://" to Redis connection string

# =============================================================================
# External API Rate Limits
# =============================================================================
# These control throttling for external API calls to prevent exceeding provider limits
# Values are in requests per second
# Note: These are currently configured in config/config.py as constants
YFINANCE_RATE_LIMIT=2.0
OPENAI_RATE_LIMIT=10.0
DEFAULT_API_RATE_LIMIT=1.0
API_THROTTLER_CAPACITY=10

# =============================================================================
# Langfuse Integration (Existing)
# =============================================================================
LANGFUSE_PUBLIC_KEY=your-langfuse-public-key
LANGFUSE_SECRET_KEY=your-langfuse-secret-key
LANGFUSE_HOST=https://cloud.langfuse.com

# =============================================================================
# Security Headers
# =============================================================================
# Security headers are automatically configured in middleware/security_headers.py
# No configuration needed - headers include:
# - Content-Security-Policy
# - X-Frame-Options
# - X-Content-Type-Options
# - X-XSS-Protection
# - Strict-Transport-Security
# - Referrer-Policy
# - Permissions-Policy
